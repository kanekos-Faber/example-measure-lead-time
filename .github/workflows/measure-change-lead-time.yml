name: 初回コミットからマージまでの時間を計測してコメント
on:
  pull_request

jobs:
  lead-time:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: echo
        run: echo 'Hello world'
